Analytics for the WMDE Technical Wishes template prototypes surveys.

* `reports`: Notebooks exploring the results.
* `hive`: Hive queries to refine data.

## Running
Run the queries:

    beeline -f hive/refresh_responses.hql